/*
 * ListTable Messages
 *
 * This contains all the text for the ListTable container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ListTable';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListTable container!',
  },
});
