// import { selectHorizontalFilterDomain } from '../selectors';

describe('selectHorizontalFilterDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
