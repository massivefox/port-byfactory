// import { selectFacultySectionDomain } from '../selectors';

describe('selectFacultySectionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
