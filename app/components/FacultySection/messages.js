/*
 * FacultySection Messages
 *
 * This contains all the text for the FacultySection container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FacultySection';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FacultySection container!',
  },
});
