/*
 * SubMenu Messages
 *
 * This contains all the text for the SubMenu container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SubMenu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SubMenu container!',
  },
});
