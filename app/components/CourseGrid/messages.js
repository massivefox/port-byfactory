/*
 * CourseGrid Messages
 *
 * This contains all the text for the CourseGrid container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CourseGrid';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CourseGrid container!',
  },
});
