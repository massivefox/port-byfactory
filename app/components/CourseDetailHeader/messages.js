/*
 * CourseDetail Messages
 *
 * This contains all the text for the CourseDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CourseDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CourseDetail container!',
  },
});
