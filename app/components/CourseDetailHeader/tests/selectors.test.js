// import { selectCourseDetailDomain } from '../selectors';

describe('selectCourseDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
