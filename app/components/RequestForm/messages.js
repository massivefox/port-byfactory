/*
 * RequestForm Messages
 *
 * This contains all the text for the RequestForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RequestForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RequestForm container!',
  },
});
