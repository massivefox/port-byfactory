/*
 * CourseDetailContent Messages
 *
 * This contains all the text for the CourseDetailContent container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CourseDetailContent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CourseDetailContent container!',
  },
});
