/*
 * Curriculum Messages
 *
 * This contains all the text for the Curriculum container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Curriculum';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Curriculum container!',
  },
});
