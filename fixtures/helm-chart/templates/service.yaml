apiVersion: v1
kind: Service
metadata:
  name: {{ include "assist-nft.fullname" . }}
  labels:
    {{- include "assist-nft.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "assist-nft.selectorLabels" . | nindent 4 }}
