/*
 * ProposalDetail Messages
 *
 * This contains all the text for the ProposalDetail container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProposalDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProposalDetail container!',
  },
});
