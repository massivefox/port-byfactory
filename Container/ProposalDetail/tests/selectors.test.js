// import { selectProposalDetailDomain } from '../selectors';

describe('selectProposalDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
