// import { selectProposalSubmitDomain } from '../selectors';

describe('selectProposalSubmitDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
