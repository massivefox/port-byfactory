// import { selectAccountProfileDomain } from '../selectors';

describe('selectAccountProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
